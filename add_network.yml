- hosts: checkpoint
  connection: httpapi

  tasks:
    - name: add network
      cp_network:
        name: "Demo day object12"
        subnet: "3.1.1.0"
        mask_length: 24
        state: "present"
        version: "1.2"
        nat_settings:
          auto_rule: true
          ipv4_address: 192.3.3.3
        auto_publish_session: true
        #comments: "comment333"
      register: add_network_response
      
    #- name: publish
    #  cp_publish:
    #  register: publish_response

    - debug:
        var: add_network_response
     